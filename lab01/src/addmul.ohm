AddMul {
    AddExp
    = AddExp "+" MulExp  -- plus
    | MulExp

    MulExp
    = MulExp "*" PriExp  -- times
    | PriExp

    PriExp
    = "(" AddExp ")"  -- paren
    | number

    number
    =  digit+             -- whole
}